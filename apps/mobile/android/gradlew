#!/bin/sh
# Gradle wrapper script (generated by Flutter init)
DIR="$(cd "$(dirname "$0")" && pwd)"
APP_HOME="$DIR"
exec java "-Xmx4g" "-Dorg.gradle.appname=$(basename "$0")" -classpath "$APP_HOME/gradle/wrapper/gradle-wrapper.jar" org.gradle.wrapper.GradleWrapperMain "$@"
